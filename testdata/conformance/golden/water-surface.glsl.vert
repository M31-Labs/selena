attribute float a_vertexIndex;
uniform mat4 mvp;
uniform mat3 normalMatrix;
uniform float poolWidth;
uniform float poolLength;
uniform float poolHeight;
uniform float normalScale;
uniform float gridResolution;
uniform float objectKind;
uniform float objectSubtype;
uniform float objectCount;
uniform float objectRadius;
uniform float opticsEnable;
uniform float opticsCaustic;
uniform float meshTextureEnable;
uniform vec3 lightDir;
uniform vec3 cameraPos;
uniform vec3 waterColor;
uniform vec3 objectCenter;
uniform vec3 objectHalf;
uniform mat4 refractionMatrix;
uniform mat4 reflectionMatrix;
uniform vec4 spheres[32];
uniform highp sampler2D stateTex;
varying vec3 vWorldPos;
varying vec2 vUv;

void main() {
  float vertexIndex = a_vertexIndex;
  float fi = float(vertexIndex);
  float res = max(gridResolution, 1.0);
  float cells = max((res - 1.0), 1.0);
  float quad = floor((fi / 6.0));
  float corn = (fi - (quad * 6.0));
  float cellY = floor((quad / cells));
  float cellX = (quad - (cellY * cells));
  float ox = ((((corn == 1.0) || (corn == 2.0)) || (corn == 4.0)) ? 1.0 : 0.0);
  float oy = ((((corn == 2.0) || (corn == 4.0)) || (corn == 5.0)) ? 1.0 : 0.0);
  float gx = min((cellX + ox), (res - 1.0));
  float gy = min((cellY + oy), (res - 1.0));
  float uvx = (gx / cells);
  float uvy = (gy / cells);
  vec2 uv = vec2(uvx, uvy);
  vec4 info = texture2DLod(stateTex, uv, 0.0);
  vec3 wp = vec3((((uvx - 0.5) * poolWidth) * 2.0), (info.x * poolHeight), (((uvy - 0.5) * poolLength) * 2.0));
  vWorldPos = wp;
  vUv = uv;
  gl_Position = (mvp * vec4(wp, 1.0));
}
